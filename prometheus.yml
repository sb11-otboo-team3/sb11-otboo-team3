global:
  scrape_interval: 5s

scrape_configs:
  - job_name: otboo
    metrics_path: /actuator/prometheus
    static_configs:
      # 컨테이너 안에서 호스트(로컬에서 ./gradlew bootRun으로 띄운 앱)로 접근하기 위한 특수 DNS 이름.
      # compose.yaml의 extra_hosts 설정과 짝을 이룸.
      - targets: ["host.docker.internal:8080"]
