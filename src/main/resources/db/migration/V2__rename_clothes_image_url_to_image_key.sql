-- ============================================================
-- 의상 도메인: image_url -> image_key 컬럼명 변경
-- 공통 S3 모듈(#81)에서 DB에는 URL이 아니라 Object Key를 저장하기로 확정되어
-- 실제 저장값 의미에 맞게 컬럼명을 정정한다.
-- ============================================================

ALTER TABLE clothes RENAME COLUMN image_url TO image_key;