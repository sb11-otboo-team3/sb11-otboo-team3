spring:
  application:
    name: otboo

  profiles:
    default: local

  jpa:
    open-in-view: false

  batch:
    job:
      enabled: false




kakao:
  local:
    api-key: ${KAKAO_REST_API_KEY}
    base-url: https://dapi.kakao.com