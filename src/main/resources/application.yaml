spring:
  application:
    name: otboo

  profiles:
    default: local

  jpa:
    open-in-view: false

  batch:
    job:
      enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health

  endpoint:
    health:
      show-details: never
      show-components: never
